# PRODUCTION Environment Configuration for Avina
#
# INSTRUCTIONS:
# 1. Copy this file to the project root directory.
# 2. Rename it to '.env'.
# 3. Fill in the placeholder values, especially the password.

# --- Core Settings ---
# Domain should point to the Production server's IP address.
DOMAIN=prd.avina.nkr.co.il
CERTBOT_EMAIL=prod-alerts@nkr.co.il

# --- MongoDB ---
MONGO_ROOT_USER=AvinaProd
MONGO_ROOT_PASSWORD=ReplaceWithA_VERY_StrongProductionPassword
MONGO_DATABASE=avina_prod

# --- Common ---
# User and Group ID for the n8n container. Run 'id -u' and 'id -g' on the host.
N8N_UID=1000
N8N_GID=1000

# Prefixes all container/network names with 'avina_prod' to avoid collisions.
COMPOSE_PROJECT_NAME=avina_prod
